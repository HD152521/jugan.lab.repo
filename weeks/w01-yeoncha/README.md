# w01 - 연차술사

> 이 폴더는 **이번 주 목표와 계획**을 적는 곳. 실제 앱 코드는 [`apps/yeoncha`](../../apps/yeoncha)에 있다.

## 이번 주 목표
- weekly.ship 시즌1의 첫 실험을 "완성해서 배포"까지 끝낸다.
- 직장인이 1초 만에 "내 연차로 며칠 쉴 수 있나"를 확인하고 공유하게 만든다.

## 뭘 만들까 (계획)
- **핵심 질문 1개에 집중**: "연차 N개 → 최장 연휴 며칠?"
- 입력은 딱 두 개 — 연차 개수(1~5), 기간(전체/2026/2027).
- 출력은 연휴 길이순 카드. 각 카드에 연차 쓰는 날짜, 효율(연차 1개당 일수), D-day.
- 바이럴 훅: **공유하기**와 **캘린더(ICS) 등록**으로 마찰 없이 퍼지게.
- 데이터는 2026 하반기~2027 공휴일. 임시공휴일은 발표 시 추가.

## 만드는 순서
1. 공휴일 데이터 확정 (`holidays.js`) — 대체공휴일 포함, 근로자의 날 제외
2. 연휴 탐색 알고리즘 (`optimizer.js`) — 극대 연휴 구간 탐색, 순수 함수로 테스트 가능하게
3. UI (`index.html` + `app.js`) — 카드/공유/ICS
4. 순수 함수 테스트 (`node --test`)
5. Vercel 배포 후 `apps/hub` 링크 연결

## 완료 기준 (DoD)
- [x] `node --test` 그린 (21 케이스)
- [x] 모바일 320px에서 가로 스크롤 깨짐 없음
- [x] 공유/ICS 동작 확인
- [x] Vercel 배포 + hub 1번 실험 링크 연결 (`yeoncha.juganlab.com`)

## 실제 출시 상태 (완료) — 관심 끄는 두 축 다 충족
계획을 넘어서 리텐션·바이럴 훅까지 붙였다.
- **세 모드**: 연휴 찾기(최장) / 연차 플래너(최고 효율, 남는 연차는 "자유롭게") / 📋 내 플랜
- 🔁 **리텐션**: **내 플랜(가계부)** — 연휴 담기, 사용/남은 연차·확보 휴일 추적, 겹침·초과 경고, 전체 캘린더 등록
- 🚀 **바이럴**: **친구 맞추기** — 플랜을 URL로 공유 → 링크 진입 시 "둘 다 쉬는 날" 교집합 / **이미지 카드**(PNG) / 효율 등급·칭호
- **회사 쉬는날 추가**(창립기념일 등), 설·추석 빼고, 연차 숫자 입력

## 배운 것 / 메모
- 알고리즘을 순수 함수로 떼어내니 브라우저 코드 없이 `node --test`로 바로 검증됨.
- 가짜 DOM 하니스로 UI 배선까지 검증하니 실제 버그(스냅샷 하이드레이트, 명절 필터 순서)를 조기에 잡음.
- "일회성 계산기"를 벗어나려면 **리텐션(내 플랜) 또는 바이럴(친구 맞추기)** 훅이 핵심 — 이후 실험의 기준선.
